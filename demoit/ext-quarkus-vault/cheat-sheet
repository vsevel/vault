start postgres:

kubectl apply -f postgres.yaml -n default

build app:

./mvnw clean package
docker build -f src/main/docker/Dockerfile.jvm -t quarkusapp .

deploy:

kubectl apply -f quarkusapp.yaml -n default
kubectl logs quarkusapp -n default --follow

